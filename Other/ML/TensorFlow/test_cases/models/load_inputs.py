#####################
## Used to define load data parameters, they change based on dataset.
#####################
label_index = [3];
one_hot_depth = 2;
keys_index = [];
record_defaults = [[0.0], [0.0], [0.0], [0.0]]; # Default values, in case of empty columns. Also specifies the type of the decoded result.